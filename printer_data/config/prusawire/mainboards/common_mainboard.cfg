[heater_bed]
sensor_type: Generic 3950
min_temp: 0
max_temp: 120

[stepper_x]
microsteps: 16
full_steps_per_rotation: 200
# Only valid for 20 teeth timing pulleys.
# The stock MK3/MK4 ones use 16 teeth pulleys that must be changed or overwritten with a rotation_distance of 32.
# Also apply to stepper_y and stepper_z.
rotation_distance: 40
position_endstop: -4
position_max: 250
position_min: -4
homing_speed: 40
homing_retract_dist: 0

[stepper_y]
microsteps: 16
full_steps_per_rotation: 200
rotation_distance: 40
position_endstop: -33
position_max: 210
position_min: -33
homing_speed: 40
homing_retract_dist: 0

[stepper_z]
microsteps: 16
full_steps_per_rotation: 200
rotation_distance: 40
step_pin: PB0
dir_pin: PC5
enable_pin: !PB1
position_max: 183
position_min: -5
homing_speed: 35
endstop_pin: probe:z_virtual_endstop

[homing_override]
axes: xyz
gcode:
    {% set home_all = 'X' not in params and 'Y' not in params and 'Z' not in params %}

    {% if 'x' not in printer.toolhead.homed_axes or 'y' not in printer.toolhead.homed_axes %}
        SET_KINEMATIC_POSITION Z=1 SET_HOMED=Z
        G1 Z10 F1200
    {% endif %}

    {% if home_all or 'X' in params %}
        SET_KINEMATIC_POSITION X=0
        _HOME_X
    {% endif %}

    {% if home_all or 'Y' in params %}
        SET_KINEMATIC_POSITION Y=0
        _HOME_Y
    {% endif %}

    {% if home_all or 'Z' in params %}
        SET_KINEMATIC_POSITION Z=0
        G90
        G0 X{printer.toolhead.axis_maximum.x|float / 2.0} Y{printer.toolhead.axis_maximum.y|float / 2.0} F7800
        G91
        G28 Z
        G91
        G0 Z10
    {% endif %}

[gcode_macro _HOME_X]
gcode:
    # Always use consistent run_current on A/B steppers during sensorless homing
    {% set DRIVER_TYPE = params.DRIVER|default(driver_type)|string %}
    {% set HOME_CURRENT_DIVISOR = params.DIVISOR|default(home_current_divisor)|float %}
    {% set RUN_CURRENT_X = printer.configfile.settings[DRIVER_TYPE ~ ' stepper_x'].run_current|float %}
    {% set RUN_CURRENT_Y = printer.configfile.settings[DRIVER_TYPE ~ ' stepper_y'].run_current|float %}
    {% set HOME_CURRENT_X = RUN_CURRENT_X / HOME_CURRENT_DIVISOR %}
    {% set HOME_CURRENT_Y = RUN_CURRENT_Y / HOME_CURRENT_DIVISOR %}
    
    SET_KINEMATIC_POSITION X=0 SET_HOMED=X
    G1 X10 F1200
    
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT_Y}

    # Home
    G28 X
    # Move away
    G91
    G1 X5 F1200
    
    # Wait just a second (give StallGuard registers time to clear)
    G4 P250
    
    # Set current during print
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}

[gcode_macro _HOME_Y]
gcode:
    # Set current for sensorless homing
    {% set DRIVER_TYPE = params.DRIVER|default(driver_type)|string %}
    {% set HOME_CURRENT_DIVISOR = params.DIVISOR|default(home_current_divisor)|float %}
    {% set RUN_CURRENT_X = printer.configfile.settings[DRIVER_TYPE ~ ' stepper_x'].run_current|float %}
    {% set RUN_CURRENT_Y = printer.configfile.settings[DRIVER_TYPE ~ ' stepper_y'].run_current|float %}
    {% set HOME_CURRENT_X = RUN_CURRENT_X / 2 %}
    {% set HOME_CURRENT_Y = RUN_CURRENT_Y / 2 %}
    
    SET_KINEMATIC_POSITION Y=0 SET_HOMED=Y
    G1 Y10 F1200
    
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={HOME_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={HOME_CURRENT_Y}

    # Home
    G28 Y
    # Move away
    G91
    G1 Y5 F1200

    # Wait just a second (give StallGuard registers time to clear)
    G4 P250
    
    # Set current during print
    SET_TMC_CURRENT STEPPER=stepper_x CURRENT={RUN_CURRENT_X}
    SET_TMC_CURRENT STEPPER=stepper_y CURRENT={RUN_CURRENT_Y}